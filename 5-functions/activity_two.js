const maximum = function (num1, num2) {
  return num1 > num2 ? num1 : num2;
};

// console.log(maximum(2,5));

const concatenate = function(str1, str2){
    return str1 + " " + str2
}

console.log(concatenate("Hello", "How are you?"));
